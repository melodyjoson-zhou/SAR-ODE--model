# SAR-ODE--model
We have prepared a complete repository package containing: model code (sar_ode_model.py, sar_ode_model_v2.py), analysis scripts (model comparison, calibration/validation, virtual cohort generation, Sobol analysis, PCA-reduced K-means clustering, revision simulations), parameter files, virtual cohort data, and all analysis output CSVs.
